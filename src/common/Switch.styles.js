import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  switch: {
    display: 'flex',
    alignItems: 'flex-start',
    justifyContent: 'flex-start',
  }
});

export default styles.switch;
