import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 200,
  },
  image: {
    height: 200,
    width: null,
    flex: 1,
  },
  icon: {
    width: null,
    fontSize: 60
  }
});

export default styles;
